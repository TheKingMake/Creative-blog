import { motion } from "framer-motion";
import {
  ArrowRight,
  Search,
  Moon,
  Menu,
  Sparkles,
  Clock,
} from "lucide-react";
import { useState } from "react";

function Home() {
 const [subscribed, setSubscribed] = useState(false); // Your existing homepage code
}

export default Home;